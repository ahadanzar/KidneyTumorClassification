1) To instal dependencies,
Go to folder that contains 'requirements.txt' file (By default main folder)
Right click and open terminal.
Run: pip install -r requirements.txt

2) To run code : Either use any appropriate IDE or open cmd and run: python kidney.py
3) Make sure to have all the same column names as the traindata file for testing files.